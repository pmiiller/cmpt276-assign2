<p id="notice"><%= notice %></p>

<%= form_for(@battle) do |f| %>
  <% if @battle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@battle.errors.count, "error") %> prohibited this battle from being saved:</h2>

      <ul>
      <% @battle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tokimon_1 %><br>
    <%= f.collection_select :tokimon1, Tokimon.order(:name),:id,:name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :tokimon_2 %><br>
    <%= f.collection_select :tokimon2, Tokimon.order(:name),:id,:name, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
